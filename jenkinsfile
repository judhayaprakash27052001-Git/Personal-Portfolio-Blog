@Library('shared-lib') _

pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                buildImage()
            }
        }

        stage('Test') {
            steps {
                testContainer()
            }
        }

        stage('Deploy') {
            when {
                branch 'main'
            }
            steps {
                deployContainer()
            }
        }
    }
}
